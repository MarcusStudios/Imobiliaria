import { describe, it, expect } from 'vitest';

describe('Configuração Inicial do Vitest', () => {
  it('garante que o ambiente de testes foi carregado corretamente', () => {
    expect(true).toBe(true);
  });
});
